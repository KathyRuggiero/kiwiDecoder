% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_directories.R
\name{scan_directories}
\alias{scan_directories}
\title{Scan Directories for Image Files and Write Per-Directory Index}
\usage{
scan_directories(
  root,
  extensions = c("jpg", "jpeg", "png"),
  prefixes = c("^PMC_[0-9]{5}$", "^DSC_[0-9]{4}$", "^IMG_[0-9]{4}$", "^P[0-9]{7}$"),
  checkpoint_file = "scan_checkpoint.rds",
  resume = TRUE,
  dry_run = FALSE,
  log_file = file.path(root, "scan_log.csv"),
  max_path_length = 255
)
}
\arguments{
\item{root}{Character scalar. Root directory for scanning.}

\item{extensions}{Character vector of allowed image file extensions (case-insensitive). Defaults to \code{c("jpg", "jpeg", "png")}.}

\item{prefixes}{Character vector of regex patterns matched against filename stems (without extensions). Use `NULL` or `character(0)` to disable stem filtering.}

\item{checkpoint_file}{File path to RDS checkpoint. Default `"scan_checkpoint.rds"`.}

\item{resume}{Logical; if `TRUE`, resumes from the checkpoint file.}

\item{dry_run}{Logical; if `TRUE`, no Excel files are written, but scanning proceeds and logs are updated.}

\item{log_file}{Optional path to a CSV log file recording scan progress and long-path warnings. Default is `"scan_log.csv"` in `root`.}

\item{max_path_length}{Maximum allowed path + filename length. Defaults to 255.}
}
\value{
Invisibly returns a list containing:
* `dirs_scanned` – character vector of directories fully scanned.
* `long_path_warnings` – data frame of directories where Excel paths exceeded `max_path_length`.
}
\description{
`scan_directories()` performs a **depth-first scan** of a directory tree,
optionally resuming from a checkpoint, and writes per-directory Excel indexes
of image files using `write_directory_index()`. It is designed for **network-mounted drives**
where naive recursive scans can be very slow or unstable.
}
\details{
Features:
* Depth-first traversal: completes one full subdirectory tree before moving to the next.
* Resumable scans via checkpointing (`checkpoint_file`).
* Dry-run mode: scan and report without writing Excel files.
* Logging of scan progress instead of printing to console.
* Detection and logging of paths that are too long for Excel.
}
